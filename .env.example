# ============================================================================
# BLACK NOVEMBER - CONFIGURAÇÃO DE VARIÁVEIS DE AMBIENTE
# ============================================================================
# 
# INSTRUÇÕES:
# 1. Copie este arquivo para .env: cp .env.example .env
# 2. Preencha os valores abaixo com suas credenciais
# 3. NUNCA commite o arquivo .env no Git (já está no .gitignore)
#
# ============================================================================

# ----------------------------------------------------------------------------
# APLICAÇÃO
# ----------------------------------------------------------------------------
PORT=5000
SECRET_KEY=your-secret-key-here-change-in-production
API_BASE_URL=http://127.0.0.1:5000

# ----------------------------------------------------------------------------
# BANCO DE DADOS POSTGRESQL (Cloud SQL)
# ----------------------------------------------------------------------------
# Credenciais do banco de dados PostgreSQL que espelha o HubSpot
PG_HOST=seu-host-postgresql
PG_PORT=5432
PG_DATABASE_HUBSPOT=hubspot-sync
PG_USER=seu_usuario
PG_PASSWORD=sua_senha

# Certificado SSL do Cloud SQL (opcional, mas recomendado em produção)
# Se estiver usando Cloud SQL, copie o conteúdo do arquivo server-ca.pem aqui
# CLOUD_SQL_CA_CERT=-----BEGIN CERTIFICATE-----\n...\n-----END CERTIFICATE-----

# ----------------------------------------------------------------------------
# HUBSPOT API
# ----------------------------------------------------------------------------
# Token de Private App do HubSpot
# Obtenha em: HubSpot > Settings > Integrations > Private Apps
HUBSPOT_PRIVATE_APP_TOKEN=seu_token_private_app_hubspot

# Secret para validar webhooks do HubSpot (opcional, mas recomendado)
# Configure no workflow do HubSpot e use o mesmo valor aqui
HUBSPOT_WEBHOOK_SECRET=seu_webhook_secret_aqui

# ----------------------------------------------------------------------------
# LOOKER (Dashboard Analytics)
# ----------------------------------------------------------------------------
# Credenciais para acessar o Looker Dashboard
# IMPORTANTE: Execute setup_looker_session.py após configurar estas variáveis
LOOKER_USERNAME=seu_email@logcomex.com
LOOKER_PASSWORD=sua_senha_looker

# Bucket do Google Cloud Storage para armazenar cookies do Looker (opcional)
# Se não configurar, os cookies serão salvos apenas localmente
# Para usar no Cloud Run, configure este bucket
GCS_BUCKET_NAME=seu-bucket-looker-cookies

# ----------------------------------------------------------------------------
# EVOLUTION API (WhatsApp)
# ----------------------------------------------------------------------------
# URL da Evolution API para envio de mensagens WhatsApp
EVOLUTION_API_URL=https://evolution-api-logcomex.34-49-195-55.nip.io

# API Key da Evolution API
# Obtenha com o administrador da Evolution API
EVOLUTION_API_KEY=sua_evolution_api_key

# Nome da instância do WhatsApp na Evolution API
EVOLUTION_INSTANCE_NAME=RevOps

# ID do grupo do WhatsApp para receber notificações
# Formato: número-grupo@g.us
ID_GRUPO_REVOPS=120363392188349999@g.us

# ID do grupo de teste (opcional, para testes)
ID_GRUPO_TESTE=seu_grupo_teste@g.us

# ----------------------------------------------------------------------------
# GOOGLE OAUTH (Autenticação)
# ----------------------------------------------------------------------------
# Credenciais OAuth do Google para login
# Configure em: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=seu_google_client_id
GOOGLE_CLIENT_SECRET=seu_google_client_secret

# URL de redirecionamento após login (opcional)
# Se não configurar, será gerada automaticamente
GOOGLE_REDIRECT_URI=https://seu-dominio.com/auth/callback

# ============================================================================
# NOTAS IMPORTANTES:
# ============================================================================
#
# 1. LOOKER SETUP:
#    Após configurar LOOKER_USERNAME e LOOKER_PASSWORD, execute:
#    python setup_looker_session.py
#    Isso abrirá um navegador para você fazer login manualmente e salvar os cookies.
#
# 2. HUBSPOT WEBHOOK:
#    Configure o webhook no HubSpot para apontar para:
#    https://seu-dominio.com/api/webhook/deal-won
#    Use o HUBSPOT_WEBHOOK_SECRET para validação.
#
# 3. EVOLUTION API:
#    Certifique-se de que a instância do WhatsApp está ativa e conectada.
#    Teste o envio de mensagens antes de usar em produção.
#
# 4. PRODUÇÃO:
#    Em produção (Cloud Run), configure estas variáveis no Secret Manager
#    ou nas variáveis de ambiente do Cloud Run.
#
# ============================================================================
